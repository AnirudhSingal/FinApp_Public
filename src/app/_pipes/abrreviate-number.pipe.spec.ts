import { AbrreviateNumberPipe } from './abrreviate-number.pipe';

describe('AbrreviateNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AbrreviateNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
